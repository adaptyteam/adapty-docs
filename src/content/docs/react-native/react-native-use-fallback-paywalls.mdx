---
title: "Use fallback paywalls in React Native"
description: "Set up fallback paywalls in your React Native project to handle offline scenarios."
metadataTitle: "Using Fallback Paywalls in React Native | Adapty Docs"
keywords: ['fallback paywalls', 'react native', 'expo', 'pure react native', 'offline paywalls']
---
import CustomDocCardList from '@site/src/components/CustomDocCardList';

Adapty's fallback paywalls work in both **Expo** and **pure React Native** projects. Because these environments use different build systems, the setup steps also differ.

Choose the setup guide that matches your project:

<CustomDocCardList />
