---
title: "Create a service account in Google Cloud Console"
description: "Create the Google account Adapty signs in with, and grant it the two Cloud roles it needs."
metadataTitle: "Create a service account in Google Cloud Console | Adapty Docs"
---

A service account is a special Google account that Adapty will use to access your Google projects and apps.

## Before you start

- **Previous step**: [Enable the Developer APIs](enabling-of-devepoler-api)
- **Why**: The service account needs the APIs to be on before it can do anything useful.

## Create the service account

1. Open [**IAM & Admin** > **Service accounts**](https://console.cloud.google.com/iam-admin/serviceaccounts) in Google Cloud Console, and confirm you're in the right project.

   <ZoomImage id="gp-service-accounts-list.webp" width="700px" alt="Service accounts page in Google Cloud Console with the Create service account button" />

2. Click **Create service account**.

3. Enter a **Service account name**. Include "Adapty" in it to make the account's purpose obvious. The **Service account ID** field fills in automatically.

4. Copy the account's **Email address**. You will use it to share the necessary Play Console permissions.

   <ZoomImage id="gp-service-account-details.webp" width="700px" alt="Create service account form showing the name, ID, and generated email address" />

5. Click **Create and continue**.

6. Give the newly created account the **Pub/Sub Admin** role. Adapty needs it to receive real-time developer notifications. Click **Select a role**, then **Pub/Sub** > **Pub/Sub Admin**.

   <ZoomImage id="gp-service-account-role.webp" width="700px" alt="Role picker in the Create service account wizard" />

7. Give it the **Monitoring Viewer** role. Adapty needs it to monitor the notification queue. Click **Add another role**, then **Monitoring** > **Monitoring Viewer**.

8. Click **Continue**.

9. Skip **Principals with access**, and click **Done**.

## Next step

- **Continue with**: [Grant permissions to the service account](grant-permissions-to-service-account)
- **Why**: The newly created service account needs Google Play Console access.