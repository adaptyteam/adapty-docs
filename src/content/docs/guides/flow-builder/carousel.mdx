---
title: "Carousels and Bottom sheets"
description: "Add swipeable elements to your flows."
metadataTitle: "Carousels and Bottom sheets | Flow Builder | Adapty Docs"
---



import Zoom from 'react-medium-image-zoom';
import 'react-medium-image-zoom/dist/styles.css';


## Carousels

A horizontal scrolling container used for displaying lists of items like Reviews, Features, or Plans.


The Inspector panel for the Carousel requires specific customization compared to standard containers.

Carousel Settings (Custom Block):

Show Dots (Toggle): Controls visibility of pagination indicators.
Snap Alignment (Segmented Control): Start (align left) / Center (align middle).
+ Add Item (Button): Adds a new Slide Item to the end of the list.

Layout:

⚠️ Constraint: Direction must be locked to Horizontal.
⚠️ Constraint: Wrap must be hidden

Child: Has  properties like regular stack. 

A carousel is a dynamic set of swipeable cards that can be moved left or right, creating a captivating visual experience. It's a fantastic tool to craft paywalls that not only draw attention but also engage users with interactive content.

<Zoom>
  <img src="/assets/shared/img/81eaa26-PB_carousel.gif"
  style={{
    border: 'none', /* border width and color */
    width: '200px', /* image width */
    display: 'block', /* for alignment */
    margin: '0 auto' /* center alignment */
  }}
/>
</Zoom>

:::warning

Carousels are only available in the [new Paywall Builder](adapty-paywall-builder), which works with iOS, Android, and React Native SDKs version 3.0 or higher, and Flutter and Unity SDKs version 3.3.0 or higher. The legacy Paywall Builder with Adapty SDK v2.x or earlier does not support paywall carousel functionality.

:::

Use the power of carousels to elevate your paywall's appeal:

1. **Add Cards**: Start by adding cards to your carousel, each one a canvas for your creative touch.
2. **Customize Cards**: Populate your cards with various elements—images, text, buttons, and more. The carousel's height setting ensures all cards are uniformly sized, delivering a sleek and consistent appearance.

Embrace the flexibility of carousels to showcase featured products, highlight exclusive offers, or narrate compelling stories. With this engaging element, your paywalls will not only stand out but also provide a seamless and immersive user experience.


### Show dots

### Snap and snap alignment

### Pagination

## Bottom sheets

A horizontal scrolling container used for displaying lists of items like Reviews, Features, or Plans.

What can go INSIDE a Slide:
Text, Images, Buttons, Vertical Stacks, Icons - Everything.

When the user drags the Bottom Sheet onto the canvas, it appears as a layout container with a Fixed position to anchor it directly to the screen viewport. The default configuration includes a Parent Stack (set to Stretch width/height), a Shadow/Overlay layer for background dimming, and the Content Sheet itself, which is pre-aligned to the bottom. A default Z-index of 12 ensures the component remains visually above all standard screen elements.

The Inspector panel allows for precise control over modal behavior through Fixed Offset values and Z-index management. Standard dismissing is handled via pre-set interactions: the Shadow and the Close button are both configured with On Tap → Hide element to return the user to the primary paywall state. Inside the sheet, users can nest regular stacks, text, or product lists, which will inherit the fixed container's constraints while supporting internal vertical scrolling if content exceeds the sheet height.