---
title: "Element styling"
description: "Style elements in the Flow Builder."
metadataTitle: "Element styling | Flow Builder | Adapty Docs"
---


## Static styling and conditional styling

### States

### Conditional styling

## Configurable properties

### Sizing

### Borders

### Corners

Location: This section is situated within the Right Sidebar (Property Panel) under the Corners heading. 
Target Object: Applied to any element that supports a border or background (Frame, Container, Image, Button, etc.)

### Visibility

### Color

#### Gradient

#### Image / Video Background

### Opacity

### Effects

#### Shadows

#### Blur

### Scroll properties

## Animations

Configuration of looping effects that play automatically to draw user attention to the Call to Action (CTA).

Type:

None: Animation disabled (Default).
Pulse: The button rhythmically scales up and down.

Parameters — visible when a type is selected:

Scale amount (%): Intensity of the effect. The percentage by which the button grows relative to its original size (e.g., 20%).
Duration (ms): The duration of a single animation phase (speed of expansion).
Delay between loops (ms): Pause interval between repetitions. Allows defining the rhythm (e.g., a "heartbeat" vs. continuous pulsing).
Show after delay (ms): Initial delay before the animation starts. Useful for triggering the effect only after the user has spent some time on the screen.
Easing: The acceleration curve (Easy in, Linear, etc.) defining the smoothness of the motion.


## Project-wide styles

### Text

<ZoomImage id="flow-builder/flow-builder-text-styles.webp" width="300px" alt="Text styles" />

### Colors

<ZoomImage id="flow-builder/flow-builder-color-styles.webp" width="300px" alt="Color styles" />

### Add a style

## Dark mode (old content)

<ZoomImage id="flow-builder/flow-builder-dark-mode.webp" width="300px" alt="Color styles" />


Dark mode has become an essential feature for improving user experience in mobile apps, especially for users who spend long periods interacting with content. Most apps that support dark mode also choose to have two different versions of the paywall (light and dark) for consistency, and Adapty allows doing exactly that.

When dark mode is enabled, the paywall automatically matches the device's current mode: it shows the light version if the device is in light mode, and the dark one if it's in dark mode.

<Zoom>
  <img src="/assets/shared/img/darkmode.webp"
  style={{
    border: 'none', /* border width and color */
    width: '400px', /* image width */
    display: 'block', /* for alignment */
    margin: '0 auto' /* center alignment */
  }}
/>
</Zoom>
:::warning

Dark mode is supported:

- iOS: starting with v3.1.0
- Android: starting with v3.1.1
- React Native: starting with v3.1.0
- Flutter: starting with v3.2.0
- Unity: starting with v3.3.0

It's also available in fallback paywalls.

:::
  To set up dark mode for your paywall:

1. First, enable dark mode in the paywall's **Layout settings**:

<Zoom>
  <img src="/assets/shared/img/dark-mode.webp"
  style={{
    border: 'none', /* border width and color */
    width: '700px', /* image width */
    display: 'block', /* for alignment */
    margin: '0 auto' /* center alignment */
  }}
/>
</Zoom>

2. Now, you can configure light and dark modes separately. To switch to dark mode, turn on the **Dark Mode** toggle in the left paywall menu:

<Zoom>
     <img src="/assets/shared/img/dark-mode-switch.webp"
     style={{
       border: 'none', /* border width and color */
       width: '700px', /* image width */
       display: 'block', /* for alignment */
       margin: '0 auto' /* center alignment */
     }}
     />
     </Zoom>

3. Once you've switched to dark mode, you can adjust the elements as needed. Dark mode lets you use a different image or video, as well as separate color and background options.

<Zoom>
  <img src="/assets/shared/img/dark-mode-done.webp"
  style={{
    border: 'none', /* border width and color */
    width: '700px', /* image width */
    display: 'block', /* for alignment */
    margin: '0 auto' /* center alignment */
  }}
  />
  </Zoom>
