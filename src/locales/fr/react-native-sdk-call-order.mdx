---
title: "Ordre des appels dans le SDK React Native"
description: "Évitez la perte d'accès premium, les attributions manquantes et les erreurs intermittentes #2002 en appelant les méthodes du SDK Adapty dans le bon ordre."
metadataTitle: "Ordre des appels dans le SDK React Native | Documentation Adapty"
---

`adapty.activate()` doit se terminer avant tout autre appel à une méthode du SDK Adapty. Tant qu'il n'est pas résolu, le SDK n'a aucun état. Tout appel émis avant ou en parallèle d'`activate()` échoue avec [`#2002 notActivated`](react-native-handle-errors#custom-network-codes).

Si votre application authentifie les utilisateurs et que vous collectez un identifiant utilisateur après le lancement, appelez `adapty.identify()` à ce moment-là. N'appelez pas les méthodes liées aux actions utilisateur tant que `identify` n'est pas résolu. Les appels qui entrent en concurrence avec lui échouent soit avec [`#3006 profileWasChanged`](react-native-handle-errors#custom-network-codes), soit atterrissent sur le profil anonyme créé à l'activation. Dans ce cas, l'attribution, les identifiants MMP comme `appsflyer_id` et la propriété de l'installation ne sont pas toujours transférés vers le profil identifié. Si votre application n'authentifie pas les utilisateurs, ignorez `identify` et continuez à travailler avec le profil anonyme.

Les SDK MMP et analytiques (AppsFlyer, Adjust, Branch, PostHog) suivent la même règle. Initialisez-les en premier et attendez leurs callbacks d'UID avant d'appeler `adapty.activate`. Sinon, l'ID MMP est associé à un profil anonyme éphémère et n'est pas toujours transféré au profil identifié. Pour les spécificités d'AppsFlyer, consultez [AppsFlyer](appsflyer).

## Le bon ordre \{#the-correct-order\}

Votre parcours dépend de deux choses : quand vous connaissez l'identifiant utilisateur client, et si vous utilisez un MMP ou un SDK d'analyse.

- **Étapes 2 et 5** : Obligatoires pour toutes les applications. Activez le SDK, puis appelez les méthodes du SDK.
- **Étapes 1 et 3** : Requises uniquement si vous intégrez un MMP ou un SDK d'analyse (AppsFlyer, Adjust, Branch, PostHog).
- **Étape 4** : Requise uniquement si votre application authentifie les utilisateurs et collecte l'identifiant utilisateur client après le lancement.

Si vous disposez de l'identifiant utilisateur client au lancement de l'application, passez-le directement dans `activate()` (étape 2a). Ce chemin ne crée jamais de profil anonyme, donc l'étape 4 est inutile.

| Étape | Appel                                                                                                          | Quand                                                                                   | Remarques                                                                                          |
|-------|---------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------|
| 1    | Initialiser votre MMP ou SDK d'analytics (AppsFlyer, Adjust, PostHog, Branch)                                     | Au lancement de l'app, en premier                                                      | Attendre le callback UID du MMP, par exemple `getAppsFlyerUID`.                                |
| 2a   | `adapty.activate('YOUR_PUBLIC_SDK_KEY', { customerUserId: 'YOUR_USER_ID' })`                                  | Au lancement de l'app, après l'étape 1, si vous disposez de l'identifiant utilisateur             | Recommandé. Aucun profil anonyme n'est jamais créé.                                             |
| 2b   | `adapty.activate('YOUR_PUBLIC_SDK_KEY')` sans `customerUserId`                                             | Au lancement de l'app, après l'étape 1, si vous n'avez pas l'identifiant utilisateur (ou ne le collectez jamais) | Adapty crée un profil anonyme.                                                           |
| 3    | `adapty.setIntegrationIdentifier(key, value)` pour chaque MMP                                                    | Après l'étape 2, avant tout appel déclenché par une action utilisateur                                              | Obligatoire pour que les identifiants MMP soient associés au bon profil.                                               |
| 4    | `await adapty.identify('YOUR_USER_ID')`                                                                       | Après l'étape 3 (ou l'étape 2 si pas de MMP), avant l'étape 5 — uniquement sur le chemin 2b avec authentification | Toujours utiliser `await`. Les appels concurrents pendant `identify` génèrent `#3006 profileWasChanged`.          |
| 5    | `getPaywall` (`getFlow` dans le SDK v4), `getPaywallProducts`, `restorePurchases`, `makePurchase`, `updateAttribution` (`updateExternalAttribution` dans le SDK 4.1), `updateProfile`  | Après l'étape 4 si vous appelez `identify` ; sinon après l'étape 3 (ou l'étape 2 si pas de MMP)      | Ces appels nécessitent un profil stable.                                                             |

:::important
Le fait d'ignorer ces étapes entraîne une perte d'accès premium pour les utilisateurs qui reviennent, l'absence d'`appsflyer_id` sur les profils, et des paywalls renvoyés pour la mauvaise audience.
:::

## Installations via web2app et entonnoirs web \{#web2app-and-web-funnel-installs\}

Si des utilisateurs achètent via un paiement web (Stripe, Paddle) et installent ensuite l'application native, le premier `activate()` sur l'appareil crée un nouveau profil anonyme. Ce profil n'est pas lié au profil web. Si vous pouvez résoudre l'identifiant utilisateur avant le lancement de l'application (depuis votre flux d'authentification ou le référent d'installation), passez-le directement dans `activate()`. Sinon, l'achat web reste invisible sur l'appareil jusqu'à ce que vous appeliez `identify('YOUR_USER_ID')` puis `restorePurchases`.

Pour les métadonnées à envoyer avec chaque paiement web, consultez :

- [Stripe](stripe)
- [Paddle](paddle)
