---
title: "Restaurer les achats dans une application mobile avec le SDK React Native"
description: "Découvrez comment restaurer les achats dans Adapty pour garantir une expérience utilisateur fluide."
metadataTitle: "Restauration des achats dans Adapty | Documentation Adapty"
keywords: ['restorePurchases']
---

La restauration des achats sur iOS et Android est une fonctionnalité qui permet aux utilisateurs de retrouver l'accès à du contenu précédemment acheté — abonnements ou achats intégrés — sans être débités à nouveau. Elle est particulièrement utile pour les utilisateurs qui ont désinstallé et réinstallé l'application, ou changé d'appareil, et souhaitent accéder à leurs achats antérieurs sans repayer.

:::note
Si vous avez construit l'écran dans un builder, vous n'avez pas besoin de restaurer les achats manuellement. Reliez son contrôle de restauration — l'action **Restore purchases** dans le Flow & Paywall Builder, ou un lien ou bouton de restauration dans l'ancien — et le SDK effectue la restauration.
:::

Si vous avez construit l'écran vous-même, appelez la méthode `.restorePurchases()` :

```typescript showLineNumbers
try {
    const profile = await adapty.restorePurchases();
    const isSubscribed = profile.accessLevels['YOUR_ACCESS_LEVEL']?.isActive;
  
    if (isSubscribed) {
        // restore access
    }
} catch (error) {
    // handle the error
}
```

Paramètres de réponse :

| Paramètre | Description |
|---------|-----------|
| **Profile** | <p>Un objet [`AdaptyProfile`](https://react-native.adapty.io/interfaces/adaptyprofile). Ce modèle contient des informations sur les niveaux d'accès, les abonnements et les achats uniques.</p><p>Vérifiez le **statut du niveau d'accès** pour déterminer si l'utilisateur a accès à l'application.</p> |

:::info
Dans Adapty React Native SDK 4.0.3 et versions ultérieures, `restorePurchases()` se termine avec succès même lorsqu'il n'y a rien à restaurer : la méthode se résout avec le profil actuel, vérifiez donc le statut du niveau d'accès dans celui-ci. Dans les versions antérieures du SDK, la méthode échoue avec l'erreur `noPurchasesToRestore` sur Android dans ce cas.
:::

<SampleApp />