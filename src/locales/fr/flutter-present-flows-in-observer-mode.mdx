---
title: "Présenter les flows en mode Observer dans le SDK Flutter"
description: "Présentez les flows en mode Observer dans votre application Flutter tout en gérant les achats avec votre propre code."
metadataTitle: "Présentation des flows en mode Observer | Documentation Adapty"
keywords: ['mode observer', 'AdaptyUIObserverModeResolver', 'setObserverModeResolver', 'flow']
---

Si vous utilisez le [mode Observer](observer-vs-full-mode), vous êtes responsable de votre propre logique d'achat. Mais vous pouvez déléguer l'interface du paywall au [Flow & Paywall Builder](adapty-flow-builder).

Concevez l'écran dans le builder, et le SDK Adapty le restitue nativement. Mettez à jour les textes, la mise en page et les tarifs quand vous le souhaitez, sans publier de nouvelle version de l'application.

Ce guide explique comment présenter un flow et gérer les callbacks d'achat. Si Adapty gère les achats à votre place, consultez [Afficher les flows et paywalls](flutter-present-paywalls) à la place.

## Avant de commencer \{#before-you-start\}

Vous avez besoin de :

- **Adapty Flutter SDK 4.0 ou version ultérieure** : les versions antérieures ne peuvent pas afficher les flows. Pour migrer une application existante, consultez le [guide de migration](migration-to-flutter-sdk-v4).
- **`observerMode` défini sur `true`** : consultez le [guide d'installation du SDK Flutter](sdk-installation-flutter#activate-adapty-module-of-adapty-sdk).
- **Intégration initiale avec les stores** : configurez Adapty [avec l'App Store](initial_ios) et [avec Google Play](initial-android).
- **Un flow publié avec des produits** : créez-le dans le [Flow & Paywall Builder](adapty-flow-builder) et associez-y des [produits](create-product).
- **Un placement pour votre flow** : [créez un placement](create-placement) et assignez-y votre flow.
- **Le flow récupéré dans votre application** : [récupérez le flow et sa configuration](flutter-get-pb-paywalls) avant de l'afficher.

## Implémentation \{#implementation\}

En mode Observer, le SDK n'effectue pas les achats à votre place. Lorsqu'un utilisateur appuie sur le bouton d'achat ou de restauration dans un flow rendu par Adapty, le SDK appelle votre `AdaptyUIObserverModeResolver` — effectuez l'achat ou la restauration avec votre propre code à cet endroit.

1. Implémentez le `AdaptyUIObserverModeResolver` :

   ```dart showLineNumbers title="Flutter"
   class MyObserverModeResolver extends AdaptyUIObserverModeResolver {
     @override
     void observerModeDidInitiatePurchase(
       AdaptyUIFlowView view,
       AdaptyPaywallProduct product,
       void Function() onStartPurchase,
       void Function() onFinishPurchase,
     ) {
       onStartPurchase(); // the view shows its loading indicator
       // make the purchase with your own code, then:
       onFinishPurchase(); // the view hides the loading indicator
     }

     @override
     void observerModeDidInitiateRestore(
       AdaptyUIFlowView view,
       void Function() onStartRestore,
       void Function() onFinishRestore,
     ) {
       onStartRestore();
       // restore purchases with your own code, then:
       onFinishRestore();
     }
   }
   ```

`observerModeDidInitiatePurchase` vous informe que l'utilisateur a initié un achat, et `observerModeDidInitiateRestore` — que l'utilisateur a initié une restauration. Déclenchez votre flow d'achat ou de restauration personnalisé en réponse.

   N'oubliez pas non plus d'invoquer les callbacks suivants pour notifier AdaptyUI de l'avancement de l'achat ou de la restauration. Cela est nécessaire pour que le flow fonctionne correctement, notamment pour afficher le chargement, entre autres :

| Callback           | Description                                                                                        |
   | :----------------- | :------------------------------------------------------------------------------------------------- |
   | onStartPurchase()  | Ce callback doit être invoqué pour notifier AdaptyUI que l'achat a démarré.                        |
   | onFinishPurchase() | Ce callback doit être invoqué pour notifier AdaptyUI que l'achat est terminé.                      |
   | onStartRestore()   | Ce callback doit être invoqué pour notifier AdaptyUI que la restauration a démarré.                |
   | onFinishRestore()  | Ce callback doit être invoqué pour notifier AdaptyUI que la restauration est terminée.             |

2. Enregistrez le resolver avant d'afficher n'importe quel écran :

   ```dart showLineNumbers title="Flutter"
   AdaptyUI().setObserverModeResolver(MyObserverModeResolver());
   ```

3. Créez et affichez le flow view comme d'habitude : [récupérez le flow et créez sa vue](flutter-get-pb-paywalls), puis [affichez-la](flutter-present-paywalls). Aucun paramètre supplémentaire n'est nécessaire — une fois le resolver enregistré, chaque flow rendu par Adapty achemine les achats et les restaurations à travers lui.

:::warning
N'oubliez pas de [signaler la transaction et de l'associer au paywall](report-transactions-observer-mode-flutter). Sinon, Adapty ne reconnaîtra pas la transaction et ne pourra pas déterminer le paywall source de l'achat.
:::
