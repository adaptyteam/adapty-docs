---
title: "Utiliser les localisations et les codes de locale dans le SDK Android"
description: "Gérez les localisations et les codes de locale de votre application pour toucher un public mondial (Android)."
metadataTitle: "Gestion des localisations et codes de locale | Documentation Adapty"
displayed_sidebar: sdkandroid
---

## Pourquoi c'est important \{#why-this-is-important\}

Les codes de locale entrent en jeu dans plusieurs scénarios — par exemple, lorsque vous essayez de récupérer le bon paywall pour la localisation actuelle de votre application.

Les codes de locale sont complexes et peuvent varier d'une plateforme à l'autre. Adapty s'appuie donc sur un standard interne commun à toutes les plateformes prises en charge. Cela dit, leur complexité rend essentiel de bien comprendre ce que vous envoyez à notre serveur pour obtenir la bonne localisation — et ce qui se passe ensuite — afin de toujours recevoir ce que vous attendez.

## Standard des codes de locale chez Adapty \{#locale-code-standard-at-adapty\}

Pour les codes de locale, Adapty utilise une version légèrement modifiée du [standard BCP 47](https://en.wikipedia.org/wiki/IETF_language_tag) : chaque code est composé de sous-balises en minuscules, séparées par des tirets. Exemples : `en` (anglais), `pt-br` (portugais (Brésil)), `zh` (chinois simplifié), `zh-hant` (chinois traditionnel).

## Correspondance des codes de locale \{#locale-code-matching\}

Lorsqu'Adapty reçoit un appel du SDK côté client avec un code de locale et commence à rechercher la localisation correspondante d'un paywall, voici ce qui se passe :

1. La chaîne de locale reçue est convertie en minuscules et tous les underscores (`_`) sont remplacés par des tirets (`-`)
2. Nous recherchons ensuite la localisation dont le code de locale correspond exactement
3. Si aucune correspondance n'est trouvée, nous prenons la sous-chaîne avant le premier tiret (`pt` pour `pt-br`) et cherchons la localisation correspondante
4. Si aucune correspondance n'est trouvée non plus, nous renvoyons la localisation `en` par défaut

Ainsi, un appareil iOS ayant envoyé `'pt_BR'`, un appareil Android ayant envoyé `pt-BR` et un autre appareil ayant envoyé `pt-br` obtiendront le même résultat.

## Implémenter les localisations : méthode recommandée \{#implementing-localizations-recommended-way\}

Si vous vous intéressez aux localisations, vous gérez probablement déjà des fichiers de chaînes localisées dans votre projet. Dans ce cas, nous recommandons d'ajouter une paire clé-valeur avec le code de locale Adapty souhaité dans chacun de vos fichiers pour les localisations correspondantes, puis d'extraire la valeur de cette clé lors de l'appel à notre SDK, comme ceci :

```kotlin showLineNumbers
// 1. Modify your strings.xml files

/*
strings.xml - Spanish
*/
<string name="adapty_paywalls_locale">es</string>

/*
strings.xml - Portuguese (Brazil)
*/
<string name="adapty_paywalls_locale">pt-br</string>

// 2. Extract and use the locale code

val localeCode = context.getString(R.string.adapty_paywalls_locale)
// pass locale code to AdaptyUI.getViewConfiguration or Adapty.getPaywall method
```

Vous pouvez ainsi garder un contrôle total sur la localisation qui sera récupérée pour chaque utilisateur de votre application.

## Implémenter les localisations : l'autre méthode \{#implementing-localizations-the-other-way\}

Vous pouvez obtenir des résultats similaires (mais pas identiques) sans définir explicitement les codes de locale pour chaque localisation. Cela consiste à extraire un code de locale depuis d'autres objets fournis par la plateforme, comme ceci :

```kotlin showLineNumbers
val locale = if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.N)
    context.resources.configuration.locales[0]
else
    context.resources.configuration.locale

val localeCode = locale.toLanguageTag()
// pass locale code to AdaptyUI.getViewConfiguration or Adapty.getPaywall method
```

Notez que nous ne recommandons pas cette approche, car il est difficile de prédire exactement ce que le serveur d'Adapty recevra.

Si vous décidez tout de même de l'utiliser, assurez-vous d'avoir couvert tous les cas d'usage pertinents.