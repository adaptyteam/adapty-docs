<p> </p>

请求成功。响应体包含 `data` 字段，该字段封装了用户的用户画像及相关信息。

| 参数 | 类型   | 可为空             | 描述                                                         |
| ---- | ------ | ------------------ | ------------------------------------------------------------ |
| data | Object | :heavy_minus_sign: | 包含带有用户详情和元数据的 [Profile](server-side-api-objects#profile) 对象。 |

### data 对象结构 \{#data-object-structure\}

`data` 字段是用户画像的主要容器，包含以下几个字段：