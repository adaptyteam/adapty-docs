---
title: "iOS - 使用备用付费墙"
description: "处理用户离线或 Adapty 服务器不可用的情况"
metadataTitle: "在 iOS 上使用备用付费墙 | Adapty 文档"
---

import Zoom from 'react-medium-image-zoom';
import 'react-medium-image-zoom/dist/styles.css';
import Tabs from '@theme/Tabs'; 
import TabItem from '@theme/TabItem'; 

import FallbackPaywallIntroduction from '@site/src/locales/zh/reusable/FallbackPaywallIntroduction.md';

:::warning
备用付费墙需要 iOS SDK v2.11 或更高版本支持。
:::

<FallbackPaywallIntroduction />

## 配置 \{#configuration\}

1. 将备用 JSON 文件添加到您的项目包中：在 XCode 中打开 **File** 菜单，选择 **Add Files to "YourProjectName"** 选项。
2. 在获取目标付费墙或用户引导**之前**调用 `.setFallback` 方法。

<Tabs groupId="current-os" queryString>
<TabItem value="swift" label="Swift" default>

```swift showLineNumbers
do {
     if let urlPath = Bundle.main.url(forResource: fileName, withExtension: "json") {
          try await Adapty.setFallback(fileURL: urlPath)
     }
} catch {
    // handle the error
}
```
</TabItem>
<TabItem value="swift-callback" label="Swift-Callback" default>

```swift showLineNumbers
if let url = Bundle.main.url(forResource: "ios_fallback", withExtension: "json") {
     Adapty.setFallback(fileURL: url)
}
```
</TabItem>
</Tabs>

参数：

| 参数          | 描述                           |
| :---------- | :----------------------------- |
| **fileURL** | 备用配置文件的路径。 |