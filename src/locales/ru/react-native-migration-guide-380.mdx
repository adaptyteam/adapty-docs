---
title: "Миграция Adapty React Native SDK на v. 3.8"
description: "Выполните миграцию на Adapty React Native SDK v3.8 для повышения производительности и доступа к новым функциям монетизации."
metadataTitle: "Миграция на Adapty React Native SDK v3.8 | Документация Adapty"
---

Adapty SDK 3.8.0 — это major-релиз, который привнёс ряд улучшений, требующих выполнения нескольких шагов миграции с вашей стороны.

## Обновите тип входных данных для получения параметров плейсмента \{#update-input-type-for-getting-placement-params\}

`GetPaywallParamsInput` переименован в `GetPlacementParamsInput`:

```diff showLineNumbers
- type GetPaywallParamsInput = {
+ type GetPlacementParamsInput = {
   placementId: string;
   locale?: string;
   fetchPolicy?: AdaptyPlacementFetchPolicy;
   loadTimeoutMs?: number;
 }
```

## Обновите метод для резервных пейволов \{#update-fallback-method\}

Метод для установки резервных пейволов был обновлён, а тип для указания местоположения резервных данных переименован:

```diff showLineNumbers
- adapty.setFallbackPaywalls(paywallsLocation: Input.FallbackPaywallsLocation);
+ adapty.setFallback(fileLocation: Input.FileLocation);
```

## Обновите обращение к свойствам пейвола \{#update-paywall-property-access\}

Следующие свойства перемещены из `AdaptyPaywall` в `AdaptyPlacement`:

```diff showLineNumbers
- paywall.abTestName
- paywall.audienceName
- paywall.revision
- paywall.placementId
+ paywall.placement.abTestName
+ paywall.placement.audienceName
+ paywall.placement.revision
+ paywall.placement.id
```