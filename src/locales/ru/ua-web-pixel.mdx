---
title: "Настройка веб-пикселя Adapty"
description: "Установите веб-пиксель Adapty в своей веб-воронке, чтобы покупки через Stripe или Paddle атрибутировались к рекламе, которая их привлекла."
metadataTitle: "Веб-пиксель | Атрибуция Adapty | Документация Adapty"
---

Если вы привлекаете пользователей через веб-воронку, веб-пиксель Adapty связывает каждый визит в воронке с рекламой, которая его привела. Пиксель присваивает каждому посетителю идентификатор клика. Когда посетитель совершает оплату, вы передаёте этот идентификатор клика вместе с транзакцией, и Adapty Attribution сопоставляет покупку с конкретной кампанией, группой объявлений и объявлением.

Пиксель доступен для кампаний Meta Ads и TikTok for Business.

:::tip
Пиксель отслеживает только переходы по рекламе. Чтобы передавать сами покупки в атрибуцию Adapty, подключите [Stripe](ua-stripe) или [Paddle](ua-paddle).
:::

## Как это работает \{#how-it-works\}

При загрузке страницы пиксель:

- Присваивает посетителю `click_id` и сохраняет его в браузере на 7 дней.
- Захватывает рекламные сигналы из URL страницы: идентификаторы кликов Meta (`fbclid`) и TikTok (`ttclid`), идентификаторы и названия кампаний, групп объявлений и объявлений, а также параметры `utm_*`.
- Отправляет `click_id` и захваченные сигналы в Adapty в фоновом режиме.

При оформлении заказа вы прикрепляете к транзакции только `click_id`. Adapty сопоставляет его с сигналами, которые уже отправил пиксель, и автоматически определяет рекламный канал.

## Перед началом работы \{#before-you-start\}

Подключите [Meta Ads](ua-facebook) или [TikTok for Business](ua-tiktok) к атрибуции Adapty и добавьте конфигурацию кампании. Adapty генерирует фрагмент пикселя для каждой конфигурации кампании, поэтому вам понадобится одна из них, чтобы скопировать фрагмент.

## Установка пикселя \{#install-the-pixel\}

1. В Adapty Attribution откройте конфигурацию вашей кампании Meta Ads или TikTok.
2. На вкладке **General** разверните раздел **Web funnel** и скопируйте значение поля **Pixel snippet**.

<ZoomImage id="ua-web-pixel-snippet.webp" width="700px" alt="Pixel snippet field in the Web funnel section of a campaign configuration in Adapty Attribution" />

3. Вставьте сниппет в `<head>` каждой страницы вашей воронки:

```html
<script async src="https://api-ua.adapty.io/pixel/loader.js" data-code="YOUR_CODE"></script>
```

Значение `data-code` идентифицирует ваше приложение и кампанию. Это публичный идентификатор, а не секретный ключ.

## Добавьте теги к URL-адресам объявлений \{#tag-your-ad-urls\}

Пиксель считывает данные об объявлениях из URL вашей целевой страницы. Рекламные сети автоматически добавляют собственный идентификатор клика (`fbclid` или `ttclid`), который определяет сеть, но не кампанию, группу объявлений или само объявление. Чтобы атрибутировать покупки вплоть до конкретного объявления, добавьте параметры отслеживания к своим объявлениям:

1. В разделе **Web funnel** настроек кампании скопируйте значение поля **Ad URL parameters**.

<ZoomImage id="ua-web-pixel-url-params.webp" width="700px" alt="Поле Ad URL parameters в разделе Web funnel настроек кампании в Adapty Attribution" />

2. В рекламной сети вставьте значение на уровне **объявления**:
   - **Meta**: в поле **URL parameters** объявления.
   - **TikTok**: в разделе URL назначения объявления, в **Build URL parameters**.

Значение содержит макросы сети. Сеть подставляет в них реальные значения кампании, группы объявлений и объявления в момент клика.

Параметры не включают значения `utm_*`, так что вы можете сохранить собственную UTM-схему для своей аналитики — Adapty этого не требует.

## Управление согласием \{#manage-consent\}

Пиксель записывает куки первой стороны и запись в localStorage, а также считывает идентификаторы кликов по рекламе при загрузке страницы. Вы являетесь контролёром данных для вашей воронки и несёте юридическую ответственность за это.

По умолчанию пиксель запускается при загрузке страницы. Чтобы включить его только после получения согласия, добавьте `data-consent="denied"` в сниппет — пиксель не будет ничего сохранять и отправлять, пока вы не предоставите согласие из вашего баннера с куки:

```js
window.adaptyPixel.grantConsent();  // start capture
window.adaptyPixel.revokeConsent(); // clear stored state and stop
window.adaptyPixel.getConsent();    // 'granted' | 'denied'
```

:::warning
Ограничивайте пиксель по согласию для трафика из ЕС/ЕЭЗ. Правила ePrivacy требуют предварительного согласия для хранения данных в браузере, и пиксель атрибуции первой стороны не является исключением.
:::

Сниппет загружается асинхронно, поэтому `window.adaptyPixel` не существует на строке сразу после тега `<script>`. Вызывайте API согласия из колбэка баннера согласия или защитите ранний вызов проверкой `if (window.adaptyPixel) { ... }`.

## Примечания по использованию \{#usage-notes\}

- **Одностраничные воронки**: Клиентская навигация сохраняет тот же `click_id`, поэтому воронка, построенная как одностраничное приложение, работает без дополнительной настройки.
- **Стабильность Click ID**: Чтение `click_id` при загрузке страницы или на этапе оформления заказа возвращает одно и то же значение. Если посетитель заходит в воронку повторно по новому объявлению, побеждает последний клик по рекламе.
- **Продления**: Прикрепляйте `click_id` только к первой транзакции. Атрибуцию конверсий из триала и продлений подписки Adapty отслеживает через саму подписку.
