---
title: "Навык агента для работы с флоу"
description: "Установите навык flow-generator, чтобы ваш AI-инструмент мог создавать, переводить и реструктурировать флоу Adapty через Adapty CLI."
metadataTitle: "Навык агента Flow Generator | Флоу и пейволы | Документация Adapty"
---

[Навык flow-generator](https://github.com/adaptyteam/adapty-skills) позволяет AI-инструменту для написания кода создавать и редактировать ваши флоу прямо из терминала. Просто опишите, что нужно сделать, — инструмент считает конфигурацию билдера флоу через [Adapty CLI](developer-cli-quickstart), перепишет её, отрендерит превью и сохранит черновик после вашего подтверждения.

Навык — это набор инструкций, которые ваш AI-инструмент загружает по запросу. Этот навык охватывает те части конфига флоу, в которых проще всего ошибиться: какие изменения блокируют публикацию, какие ссылки ломаются при удалении экрана и что предварительный просмотр способен и не способен проверить.

:::important
Навык flow-generator экспериментальный и сохраняет изменения напрямую во флоу в вашем дашборде. Сначала запускайте его на черновике или копии и проверяйте каждое изменение в Flow & Paywall Builder перед публикацией.
:::

## Перед началом работы \{#before-you-start\}

- **AI-инструмент для написания кода с поддержкой навыков**: подойдут Claude Code, GitHub Copilot CLI, OpenAI Codex и Gemini CLI.
- **Продукты в вашем каталоге Adapty**: прежде чем разработать экран продаж, навык запрашивает список ваших продуктов и уточняет, какие из них должны быть на нём. Если подходящих нет, он может создать их на основе предоставленных вами идентификаторов продуктов в сторе — и сначала уточнит это.

## Что вы можете с этим сделать \{#what-you-can-do-with-it\}

### Создание флоу с нуля \{#author-a-flow-from-scratch\}

- **По описанию**: «Пейвол для моего фитнес-приложения, два тарифа, годовой выбран по умолчанию.» Навык подбирает композицию, собирает её из проверенных шаблонов — вкладки, карточки тарифов, переключатели, обратный отсчёт, прогресс-бары, нижние шторки — и использует продукты, которые вы выбрали из своего каталога.
- **По скриншоту или дизайну**: Дайте ему референс — и он воспроизведёт макет, цвета, типографику и стиль иконок, а затем расскажет о каждом отличии, которое не удалось устранить. Референсом может быть пейвол конкурента, один из ваших экранов или фрейм из вашего дизайн-инструмента. Если ваш AI-инструмент подключён к этому дизайн-инструменту через MCP, он читает фрейм напрямую — никому не нужно экспортировать скриншот или заново описывать дизайн словами.
- **С оценкой по конверсии**: Если вы не дали дизайн, навык прогоняет экран через библиотеку `paywall-teardown` от Adapty и исправляет то, что влияет на конверсию сильнее всего, — ещё до того, как вы это увидите. Вы получаете экран, построенный по паттернам, которые продают в вашей категории, а не универсальный макет.

### Изменение существующего флоу \{#change-a-flow-you-already-have\}

- **Локализация для новых рынков**: добавляет локаль и заполняет все локализуемые поля — заголовки, подписи к кнопкам, описания тарифов, слоты изображений и плейсхолдеры полей ввода. Это самый быстрый способ адаптировать один готовый пейвол для десяти языков.
- **Переписать текст сразу во всех локалях**: изменяет формулировки, не трогая структуру, и редактирует текст вокруг переменных цены, а не через них, — так цены продолжают корректно подставляться.
- **Изменить структуру флоу**: добавляет, удаляет и переставляет экраны, затем восстанавливает нарушенную навигацию и сообщает о недостижимых экранах.
- **Добавить квизы и ветвление**: создаёт группы с выбором, идентификаторы вариантов и условные действия, которые направляют каждый ответ на отдельный экран, — шаг персонализации, превращающий онбординг в целевой визит на пейвол.
- **Создать варианты для теста**: создаёт новый флоу на основе уже рабочего, затем применяет нужное изменение. Тема, шрифты, локали и продукты переносятся автоматически, поэтому вариант отличается ровно там, где нужно.
- **Обновить стиль под сезон или кампанию**: берёт цвета из референсного изображения и применяет новую тему ко всему флоу, вместо того чтобы переоформлять каждый экран вручную.

## Что получается на выходе \{#what-it-produces\}

### Пейвол с временной шкалой плана \{#a-paywall-with-a-plan-timeline\}

<ZoomImage id="flow-generator-timeline-paywall.webp" width="320px" alt="Сгенерированный финансовый пейвол: трёхшаговая нумерованная временная шкала, соединённая зелёной полосой, затем выбранная карточка годового плана с ценой в пересчёте на месяц рядом с годовой ценой, невыбранная карточка месячного плана и кнопка «Продолжить»" />

Нумерованная временная шкала над двумя карточками планов. Отображается на устройстве, поэтому стор уже рассчитал цены — включая стоимость в месяц для годового плана, которую вычисляет Adapty.

### Пейвол из одного предложения

<ZoomImage id="flow-generator-paywall.webp" width="320px" alt="A generated dark-themed fitness paywall: headline, three benefit rows with icons, a selected Yearly card badged Best value, an unselected Monthly card, and a Start training button" />

Задание звучало как «добавить экран пейвола» — без референсов и исходного флоу. `$0.00` — это заглушка для предпросмотра, а на бейдже написано **Best value**, потому что скидка не была указана.

### Флоу из трёх экранов \{#a-three-screen-flow\}

<ZoomImage id="flow-generator-flow-strip.webp" width="700px" alt="Три сгенерированных экрана рядом: квиз с выбором цели тренировки, где выбран второй вариант; план на восемь недель, разбитый на три фазы; и экран с предложением с обратным отсчётом над карточкой «Yearly»" />

Квиз, план, предложение — с настроенной навигацией между ними. Обратный отсчёт отрисовывается, но убедиться, что он запускается, можно только на реальном устройстве.

## Установка навыка \{#install-the-skill\}

Навык поставляется в репозитории `adapty-skills` вместе с `adapty-integration`, `flow-audit`, `migrate-placements`, `ads-manager`, `paywall-teardown` и `onboarding-teardown`. Любая из этих команд устанавливает все семь.

<Tabs>
  <TabItem value="claude-code" label="Claude Code">
    ```bash
    claude plugin marketplace add adaptyteam/adapty-skills
    claude plugin install adapty-skills@adapty
    ```

Затем выполните `/reload-plugins` в Claude Code, чтобы активировать навыки.
  </TabItem>
  <TabItem value="copilot-cli" label="Copilot CLI">
    ```bash
    git clone https://github.com/adaptyteam/adapty-skills.git
    cp -r adapty-skills/skills/* ~/.copilot/skills/
    ```
  </TabItem>
  <TabItem value="gemini-cli" label="Gemini CLI">
    ```bash
    gemini skills install https://github.com/adaptyteam/adapty-skills
    ```
  </TabItem>
  <TabItem value="codex" label="Codex">
    ```bash
    git clone https://github.com/adaptyteam/adapty-skills.git
    cp -r adapty-skills/skills/* ~/.agents/skills/
    ```
  </TabItem>
  <TabItem value="any-tool" label="Any tool">
    ```bash
    npx skills add adaptyteam/adapty-skills --all
    ```

`--all` устанавливает все скилы во все агенты, которые обнаруживает [skills CLI](https://skills.sh). Чтобы установить только этот скил, используйте `--skill flow-generator`. Скилы, установленные таким образом, не обновляются автоматически — выполните `npx skills update`, чтобы получить более новую версию.
  </TabItem>
</Tabs>

Затем вызовите его — `/flow-generator` в инструментах, которые поддерживают слеш-команды, или фразой «Use the flow-generator skill» в тех, которые не поддерживают.

Дальше всё происходит на обычном языке. Большинство запросов — это одно из следующего:

- **Опишите флоу**: что делает приложение, что продаётся на экране, какой план выбрать по умолчанию.
- **Укажите на дизайн**: скриншот, один из ваших экранов или фрейм в инструменте дизайна.
- **Попросите о конкретном изменении**: назовите флоу и изменение — новая локаль, другой текст, дополнительный экран, ветка по ответу в квизе.
- **Отвечайте на его вопросы**: он спрашивает, какие продукты разместить на экране, и не придумывает реальные цифры сам.

## Как это работает \{#how-it-works\}

Всё, что создаёт навык, сначала попадает в локальный файл. Ничего не отправляется на дашборд до последнего шага.

1. **Настройка и аутентификация**. Навык устанавливает или обновляет [Adapty CLI](developer-cli-quickstart), если нужно, затем проверяет вашу авторизацию в Adapty и считывает ID приложения. Если вы не авторизованы, открывается браузер для входа, и навык ждёт завершения.

2. **Выберите или создайте флоу**. При редактировании инструмент загружает конфигурацию флоу и сообщает, что было найдено — экраны, локали, продукты и граф навигации — чтобы ошибочный флоу был обнаружен до внесения каких-либо изменений. Сначала создаётся резервная копия загруженной конфигурации. При создании нового флоу инструмент создаёт чистый флоу, а не генерирует поверх того, в котором уже есть контент.

3. **Проверьте конфиг**. Навык выполняет две локальные проверки файла и одну через Adapty. Локальные проверки подтверждают, что внутренние ссылки корректны и свойства сформированы правильно. `adapty flows config validate` затем запускает тот же сервис, который используется при публикации, — зелёный результат означает, что конфиг готов к публикации. Ни одна из трёх проверок ничего не сохраняет.

4. **Просмотр и итерации**. Навык рендерит каждый изменённый экран как скриншот и изучает его. Если вы предоставили референсное изображение, он сравнивает рендер с файлом поэлементно — цвет, шрифт, стиль иконок, пропорции — и устраняет найденные расхождения. Всё, что выглядит неправильно, возвращает навык к шагу 3, что стоит одного скриншота, а не сохранения.

5. **Подтвердите и сохраните**. Перезапись существующего конфига требует явного подтверждения. Навык покажет вам отрендеренный вид «до и после» для каждого затронутого экрана, перечислит изменения, которые не отображаются визуально, откроет один экран в браузере и укажет имя файла резервной копии. Только после этого он запишет изменения.

## После запуска \{#after-the-run\}

Скилл сохраняет черновик. Чтобы показать его пользователям:

1. **Откройте флоу в Flow & Paywall Builder.** Если флоу уже был открыт во вкладке браузера, обновите страницу — Flow & Paywall Builder не подхватывает изменения, сделанные через CLI.
2. **Проверьте, что изменилось.** Adapty помечает флоу, который был **Published** до сохранения, как **Dirty**, и все участники команды видят этот статус. Пользователи продолжают видеть последнюю опубликованную версию.
3. **Протестируйте на реальном устройстве.** В статье [Предпросмотр флоу](paywall-device-compatibility-preview) описан процесс сканирования QR-кода через мобильное приложение Adapty. Проверьте всё, что инструмент отметил как непроверенное — ветвление, переключатели и все локали, кроме той, что была отрендерена.
4. **Опубликуйте.** Нажмите **Publish to Live** в правом верхнем углу Flow & Paywall Builder или выполните команду [`adapty flows publish`](developer-cli-reference#adapty-flows-publish) в терминале. Флоу, который не добавлен в [плейсмент](create-placement), не увидит никто, а флоу в статусе Draft нельзя добавить в плейсмент — поэтому сначала опубликуйте. См. [Сохранение и публикация флоу](builder-save-publish).

Если результат неверный, восстановите данные из резервной копии: перед первым изменением конфига скилл создаёт копию и указывает имя файла в сообщении о подтверждении. Попросите скилл записать тот файл обратно или исправьте флоу вручную в Flow & Paywall Builder. В любом случае сохранённый черновик ни на что не влияет для ваших пользователей.

## Примеры промптов \{#example-prompts\}

Навык уже содержит команды, формат конфигурации и шаги верификации, поэтому в промпте не нужно ничего из этого указывать. Просто скажите, какой флоу, и что должно быть верно по завершении.

#### Перевести пейвол \{#translate-a-paywall\}

```
Add Brazilian Portuguese to my "Summer 2026" paywall. Translate every
localizable field, not just the headline, and tell me what you couldn't.
```

#### Собрать из дизайна \{#build-from-a-design\}

```
Build a new paywall flow from the "Winter promo" frame in our Figma file.
Show me my products first and let me pick which ones go on it.
```

#### Добавить квиз с ветвлением \{#add-a-quiz-that-branches\}

```
Add a quiz screen before my paywall: "What's your main goal?" with three
options, each routing to its own benefit screen, then on to the paywall.
```

#### Переписать текст \{#rewrite-copy\}

```
Rewrite the headline and CTA on my onboarding to be shorter and more direct.
Keep the price variables working, and apply it to every locale.
```

#### Создать тестовый вариант \{#spin-up-a-test-variant\}

```
Copy my "Main paywall" into a new flow and make the monthly plan the
preselected one. Leave the original alone.
```

#### Переделать под кампанию \{#re-skin-for-a-campaign\}

```
Re-theme my paywall flow to match the colors in ./design/spring.png.
Copy and layout stay as they are.
```

#### Добавить экран \{#add-a-screen\}

```
Add a social proof screen between the quiz and the paywall in my activation
flow — star rating and two testimonials. Ask me for the real numbers.
```

## Передайте изображения

Инструмент загружает изображение с вашего компьютера и подставляет полученный URL в флоу, так что добавлять медиафайлы в Flow & Paywall Builder вручную не нужно. Ему нужен **путь к файлу**, а не картинка: когда вы вставляете или прикрепляете изображение в чат, AI-инструмент получает пиксели, но не файл — загружать нечего.

Сложите ресурсы в папку на компьютере и укажите инструменту путь к ней:

```
The hero images are in ./design/paywall/. Use hero-calm.png on the first
screen and the three icons in ./design/paywall/icons/ for the benefit rows.
```

Форматы и ограничения по размеру совпадают с теми, что действуют при ручной загрузке в Flow & Paywall Builder — подробнее в разделе [Изображения, видео и иконки](custom-media). Единственное исключение — SVG: загрузить его не получится, поэтому вместо него инструмент рисует одноцветные глифы inline.

Каждая загрузка добавляет отдельный ресурс в медиатеку, а команды CLI для удаления нет, поэтому повторный запуск сборки оставляет дубликаты.

Если указать файл, который инструмент не найдёт, он спросит об этом, а не подставит похожий. Неправильный файл даст экран, который визуально выглядит корректно, но с неверным изображением — и ни один шаг рабочего процесса это не поймает.

## Подключите своего агента \{#bring-your-own-agent\}

В Adapty также есть [AI Editor](flow-ai-editor) внутри Flow & Paywall Builder. Разница не в том, у кого больше возможностей — а в том, чья среда используется для работы.

AI Editor — это агент Adapty, который работает с открытым у вас экраном. Скилл — это противоположная схема: ваш агент, ваша модель, ваш инструментарий, а Adapty — ещё один инструмент, которым агент умеет пользоваться. Всё, к чему ваш AI-инструмент уже имеет доступ, идёт вместе с ним: ваш репозиторий, файлы дизайна, MCP-серверы, ваши собственные промпты и контекст.

| | AI Editor | навык flow-generator |
|---|---|---|
| Чей агент | Adapty, в браузере | Ваш, в вашем терминале |
| Что видит | Открытый экран | Всё, до чего может дотянуться ваш AI-инструмент |
| Охват одного промпта | Один экран | Весь флоу — экраны, навигация, локали |
| Локализация | Не предусмотрена | Добавляет локаль и заполняет все локализуемые поля |
| Отмена | Один клик на промпт | Файл резервной копии и ваше подтверждение перед каждым сохранением |
| Настройка | Не требуется | Adapty CLI и навык |

Используйте AI Editor, когда вы уже в билдере и хотите изменить стиль экрана прямо перед собой. Используйте скилл, когда не хотите покидать свою среду, чтобы поработать с флоу.

## Что скилл не делает \{#what-the-skill-wont-do\}

Скилл работает с вашими флоу как с активным контентом, поскольку опубликованный флоу обслуживает реальных пользователей.

- **Публикация остаётся за вами**: навык сохраняет только черновик и никогда не публикует его. Опубликуйте самостоятельно в Flow & Paywall Builder или с помощью [`adapty flows publish`](developer-cli-reference#adapty-flows-publish).
- **Перезапись требует вашего подтверждения**: сохранение полностью заменяет конфиг — без частичной записи и без отмены. При подтверждении указывается название флоу, его ID, статус и резервный файл, который навык создал перед редактированием.
- **Параллельное редактирование прерывает запись**: каждая запись, начиная со второй, отправляет в Adapty метку времени `updated_at` из конфига. Если в этот момент коллега уже сохранил флоу, Adapty отклоняет запись, не перезаписывая его изменения.
- **Удаление флоу выполняется только через дашборд**: в CLI нет команды удаления, поэтому флоу, созданный навыком, останется до тех пор, пока вы не удалите его из дашборда.
- **Реальные данные и ресурсы предоставляете вы**: рейтинги, количество отзывов, размеры скидок и цены в сторе никогда не придумываются. Если для элемента нужны такие данные, а вы их не указали, навык либо запросит их, либо пропустит элемент — и сообщит, что именно он сделал.

:::note
Чтение, валидация и превью ничего не стоят и ничего не меняют. Сохранение конфига — единственное действие, которое достигает вашего дашборда, и оно требует вашего подтверждения.
:::

## Ограничения \{#limitations\}

#### Запуски занимают время \{#runs-take-time\}

Навык валидирует и перерисовывает результат после каждого изменения — именно это защищает дашборд от сломанной конфигурации, но делает запуск медленнее, чем обычный ответ в чате. Небольшая правка завершается за несколько минут. Создание многоэкранного флоу с ветвлением занимает **от 10 до 40 минут**. Запустите задачу и вернитесь позже — не стоит ждать у экрана.

#### Видео и кастомные шрифты загружаются вручную \{#videos-and-custom-fonts-stay-manual\}

Навык загружает изображения, но не [видео](custom-media) и не файлы [кастомных шрифтов](using-custom-fonts-in-flow-builder). В обоих случаях он создаёт нужный слот и передаёт загрузку вам:

- **Video**: Навык размещает медиаэлемент там, где должно быть видео, так что разметка уже готова — просто загрузите файл в Flow & Paywall Builder.
- **Fonts**: Навык создаёт текстовые стили, которые нужны вашему дизайну, и привязывает их к placeholder-шрифту. Загрузите шрифт в Flow & Paywall Builder и назначьте его этим стилям.

#### Предпросмотр отображает один экран в одном состоянии \{#the-preview-renders-one-screen-in-one-state\}

Страница предварительного просмотра скила отображает только один экран и не воспроизводит флоу целиком. На ней невозможно проверить следующее. Тестируйте каждый пункт на реальном устройстве — как это сделать, описано в разделе [Предварительный просмотр флоу](paywall-device-compatibility-preview):

- **Навигация и ветвление**: кнопка с навигационным действием остаётся в превью на месте, поэтому условный маршрут не срабатывает. Каждый экран рендерится отдельно.
- **Переключатели и мультивыбор**: строка-переключатель и строка мультивыбора отображаются в выключенном состоянии согласно своему дефолту. Строки с единственным выбором и карточки продуктов рендерят выбранное состояние корректно.
- **Любой язык, отличный от отображаемого**: превью игнорирует язык флоу по умолчанию и порядок списка локалей, поэтому визуально проверить перевод невозможно. Переключите язык в Flow & Paywall Builder и найдите текст, выходящий за границы контейнера.
- **Всё, что определяется в рантайме**: реальные цены, валюта стора и пользовательский ввод. Переменная цены отображается как литеральный токен, который длиннее любой цены и может перекрыть текст закреплённой кнопкой — это наложение исчезает, как только цена подгружается.
- **Размеры экранов, неизвестные превью**: макет, безопасность которого зависит от высоты viewport, нужно проверять на реальном телефоне с небольшим экраном.

## Что дальше \{#whats-next\}

- [Навык flow-audit](flow-audit-skill) — проверка готовности черновика к публикации в режиме только для чтения, прежде чем вы его опубликуете.
- [Сохранение и публикация флоу](builder-save-publish) — завершение черновика, который оставляет навык.
- [Распространённые проблемы](flow-common-issues) — что мешает флоу опубликоваться.
- [Добавить локаль в Adapty Flow & Paywall Builder](add-paywall-locale-in-adapty-paywall-builder) — та же работа по локализации вручную.
- [Управление Adapty с помощью ИИ-агентов и инструментов для кода](manage-adapty-with-ai) — другие навыки Adapty и ИИ-инструменты.
