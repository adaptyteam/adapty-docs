---
title: "Flutter SDK'da Observer modunu uygulayın"
description: "Adapty'de observer modunu uygulayarak Flutter SDK'da kullanıcı abonelik etkinliklerini takip edin."
metadataTitle: "Flutter SDK'da Observer Modu Uygulama | Adapty Dokümanları"
---

Kendi satın alma altyapınız zaten varsa ve Adapty'ye tamamen geçmeye hazır değilseniz, [Observer mode](observer-vs-full-mode)'u inceleyebilirsiniz. Temel haliyle Observer Mode, gelişmiş analizler ve attribution ile analitik sistemleriyle sorunsuz entegrasyon sunar.
Bu ihtiyacınızı karşılıyorsa, yalnızca şunları yapmanız gerekir:
1. Adapty SDK'yı yapılandırırken `observerMode` parametresini `true` olarak ayarlayarak etkinleştirin. Kurulum talimatları için [Flutter](sdk-installation-flutter#activate-adapty-module-of-adapty-sdk) sayfasına bakın.
2. Mevcut satın alma altyapınızdan Adapty'ye [işlemleri bildirin](report-transactions-observer-mode-flutter).
## Observer modu kurulumu \{#observer-mode-setup\}

Satın alma işlemlerini ve abonelik durumunu kendiniz yönetiyorsanız ve Adapty'yi abonelik olayları ile analitik göndermek için kullanıyorsanız Observer modunu etkinleştirin.

:::important
Observer modunda çalışırken Adapty SDK hiçbir işlemi kapatmaz; bu nedenle işlemleri kendiniz yönettiğinizden emin olun.
:::

```dart showLineNumbers title="main.dart"
await Adapty().activate(
  configuration: AdaptyConfiguration(apiKey: 'YOUR_PUBLIC_SDK_KEY')
    ..withObserverMode(true) // Enable observer mode
    ..withLogLevel(AdaptyLogLevel.verbose),
);
```

Parametreler:

| Parametre                   | Açıklama                                                     |
| --------------------------- | ------------------------------------------------------------ |
| observerMode                | [Observer modunu](observer-vs-full-mode) kontrol eden bir boolean değeri. Varsayılan değer `false`'tur.  |

## Observer Mode'da Adapty flow'ları ve paywall'ları kullanma \{#using-adapty-paywalls-in-observer-mode\}

Adapty'nin flow'larını, paywall'larını ve A/B testi özelliklerini de kullanmak istiyorsanız bunu yapabilirsiniz — ancak Observer Mode'da bu, biraz ek kurulum gerektirir. Yukarıdaki adımlara ek olarak yapmanız gerekenler:

1. [Remote config paywall'larını](present-remote-config-paywalls-flutter) her zamanki gibi gösterin. Flow Builder'da oluşturulmuş flow'lar ve paywall'lar için [Flow Builder'da oluşturulan flow'ları Observer modunda sunma](flutter-present-flows-in-observer-mode) bölümüne bakın.
2. Flow'ları ve paywall'ları satın alma işlemleriyle [ilişkilendirin](report-transactions-observer-mode-flutter).

:::tip
SDK v4'te, Observer modunda Adapty tarafından render edilen flow'ları ve paywall'ları da sunabilirsiniz: kullanıcı ilgili butona dokunduğunda satın alma veya geri yükleme işlemini kendi kodunuzla gerçekleştirmek için bir `AdaptyUIObserverModeResolver` kaydedin.
:::