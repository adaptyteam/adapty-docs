---
title: "Kotlin Multiplatform - Yedek paywall kullanımı"
description: "Kullanıcılar çevrimdışıyken veya Adapty sunucularına erişilemediğinde durumu yönetin"
metadataTitle: "Kotlin Multiplatform'da Yedek Flow Kullanımı | Adapty Dokümanları"
---

import SampleApp from '@site/src/locales/tr/reusable/SampleApp.md';

import FallbackPaywallIntroduction from '@site/src/locales/tr/reusable/FallbackPaywallIntroduction.md';

<FallbackPaywallIntroduction />

## Yapılandırma \{#configuration\}

1. Yedek yapılandırma dosyasını uygulamanıza ekleyin.

    * Hedef platformunuz Android ise yedek yapılandırma dosyasını `android/app/src/main/assets/` klasörüne taşıyın.
    * Hedef platformunuz iOS ise yedek JSON dosyasını proje paketinize ekleyin. (**File** -> **Add Files to YourProjectName**)

2. Hedef flow, paywall veya onboarding'i almadan **önce** `.setFallback` metodunu çağırın.
3. Hedef platformunuza göre `assetId` parametresini ayarlayın.
    * Android: `assets` dizinine göreli dosya yolunu kullanın.
    * iOS: Tam dosya adını kullanın.

```kotlin showLineNumbers

Adapty.setFallback(assetId = "fallback.json")
    .onSuccess { 
        // Fallback paywalls loaded successfully
    }
    .onError { error ->
        // Handle the error
    }
```
:::important
`setFallback`, SDK'nın hedef flow, paywall veya onboarding'i getirmesinden önce çalıştırılmalıdır.
:::

Parametreler:

| Parametre   | Açıklama                                                                                                                                                                       |
| :---------- |:----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **assetId** | Yedek yapılandırma dosya adı (iOS). <br /> `assets` dizinine göreli yedek yapılandırma dosya yolu (Android). |

<SampleApp />
