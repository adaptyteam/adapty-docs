---
title: "Flutter SDK'da flow ve paywall çekme işlemini optimize etme"
description: "Adapty flow'larını ve paywall'ları güvenilir şekilde çekin: Flutter için zamanlama, önbellekleme ve yedek desenler."
metadataTitle: "Flow ve Paywall Çekme İşlemini Optimize Etme | Flutter SDK | Adapty Dokümanları"
---

Flutter'da güvenilir bir flow veya paywall çekme işlemi üç şeyi başarır: hızlı render eder, kitleye özel varyantı döndürür ve ağ yavaş olduğunda sorunsuz bir şekilde yedekleme yapar. Aşağıdaki kurallar, bunu sağlamak için zamanlama, önbellekleme ve yedek desenleri kapsar.

:::tip
Bu kurallar, `Adapty().activate()` ve `Adapty().identify()` fonksiyonlarının zaten tamamlandığını varsayar. Bkz. [Flutter SDK'da çağrı sırası](flutter-sdk-call-order).
:::

## Kurallar ve dikkat edilmesi gereken noktalar \{#rules-and-pitfalls\}

| Bunu yapın | Bunu yapmayın | Neden |
|---|---|---|
| Göstermek üzere olduğunuz placement'ı getirin.                                                                                  | Başlatmada tüm placement'ları aynı anda önceden getirin.        | Toplu önceden getirme, ana thread'i bloke eder ve bu sürede siyah bir ekran görünür.                                  |
| `getFlow`'u, attribution'ın çözümlenme şansı bulduktan sonra getirin — örneğin, `activate`'den 1-2 saniye sonra veya `didUpdateProfileStream` tetiklendikten sonra. | `getFlow`'u `runApp`'ten önce `main()` içinde çağırın.         | Attribution henüz gelmemiştir. Flow, varsayılan kitleye göre çözümlenir ve sessizce segmentleri ile ASA kişiselleştirmesini atlar. |
| Bir `loadTimeout` belirleyin ve her placement için bir [yedek paywall](fallback-paywalls) yapılandırın.                             | `getFlow`'u süresiz olarak bekleyin.                     | Zaman aşımı olmadan, zayıf bağlantıdaki kullanıcılar ağ çözümlenene kadar — ya da uygulamayı kapatana kadar — boş bir ekran görür.     |

[Paywall ve ürünleri getirme](fetch-paywalls-and-products-flutter) sayfasında `fetchPolicy` ve `loadTimeout` parametre referanslarına, [Placement'lar](placements) sayfasında ise doğru placement'ı seçmeye ilişkin bilgilere ulaşabilirsiniz.

## Zayıf bağlantı için ayarlama \{#tune-for-poor-connectivity\}

Sürekli zayıf bağlantının yaşandığı pazarlar için (kırsal alanlar, transit bölgeler, yönlendirme sorunları yaşanan bölgeler):

- İlk fetch hariç her fetch işleminde `fetchPolicy: AdaptyFlowFetchPolicy.returnCacheDataElseLoad` kullanın.
- Adapty Kontrol Paneli'nde her placement için bir [yedek paywall](fallback-paywalls) yapılandırın.
- `loadTimeout` değerini 3–5 saniye olarak ayarlayın ve zaman aşımı gerçekleştiğinde yedek paywalla geçin.
- Flow gösterimini `getProfile()` sonucuna bağlamayın. Yavaş bir profilin arayüzü bloke etmemesi için `getFlow` işlevini bağımsız olarak çağırın.
