---
title: "Android SDK'da Flow & Paywall Builder ile satın almaları etkinleştir"
description: "Adapty Flow & Paywall Builder ile uygulama içi satın almaları etkinleştirmek için hızlı başlangıç kılavuzu."
metadataTitle: "Adapty Flow & Paywall Builder Hızlı Başlangıç | Adapty Dokümanları"
keywords: ['flows android', 'paywalls android', 'sdk android', 'flow builder', 'getFlow']
rank: 90
---

Uygulama içi satın almaları etkinleştirmek için üç temel kavramı anlamanız gerekir:

- [**Ürünler**](product) – kullanıcıların satın alabileceği her şey (abonelikler, consumable'lar, süresiz erişim)
- [**Flow'lar**](adapty-flow-builder) – no-code Flow & Paywall Builder ile oluşturulan, kullanıcılara ürünleri sunan ekran dizileri. SDK bunları `getFlow` ile alır. Kullanıcı arayüzünü kendi kodunuzla oluşturmayı tercih ediyorsanız bunun yerine paywall kullanın — bkz. [Paywallları manuel olarak uygulama](android-quickstart-manual).
- [**Placement'lar**](placements) – uygulamanızda flow'ların nerede ve ne zaman gösterileceği (`main`, `onboarding`, `settings` gibi). Flow'ları kontrol panelindeki placement'lara bağlar, ardından kodunuzda placement ID'siyle talep edersiniz. Bu sayede A/B testleri çalıştırmak ve farklı kullanıcılara farklı flow'lar göstermek kolaylaşır.

Adapty, uygulamanızda satın almaları etkinleştirmek için size üç yöntem sunar. Uygulama gereksinimlerinize bağlı olarak birini seçin:

| Uygulama | Karmaşıklık | Ne zaman kullanılır |
|---|---|---|
| Adapty Flow & Paywall Builder | ✅ Kolay | [No-code builder'da eksiksiz, satın almaya hazır bir flow oluşturursunuz](quickstart-paywalls). Adapty bunu otomatik olarak işler ve tüm karmaşık satın alma akışını, makbuz doğrulamasını ve abonelik yönetimini arka planda halleder. |
| Manuel olarak oluşturulan paywalllar | 🟡 Orta | Paywall arayüzünüzü uygulama kodunuzda kendiniz oluşturursunuz, ancak ürün tekliflerinde esnekliği korumak için flow nesnesini Adapty'den almaya devam edersiniz. [Kılavuza](android-quickstart-manual) bakın. |
| Observer modu | 🔴 Zor | Zaten kendi satın alma işleme altyapınız var ve bunu kullanmaya devam etmek istiyorsunuz. Observer modunun Adapty'de bazı kısıtlamaları olduğunu unutmayın. [Makaleye](observer-vs-full-mode) bakın. |

:::important
**Aşağıdaki adımlar, Adapty Flow & Paywall Builder'da oluşturulan bir flow'un nasıl uygulanacağını göstermektedir.**

Paywall arayüzünü kendiniz oluşturmayı tercih ediyorsanız, bkz. [Paywallları manuel olarak uygulama](android-quickstart-manual).
:::

Adapty Flow & Paywall Builder'da oluşturulan bir flow'u göstermek için uygulama kodunuzda yalnızca şunları yapmanız gerekir:

1. **Flow'u alın**: Adapty'den alın.
2. **Gösterin, satın alma işlemlerini Adapty halledecek**: View'u uygulamanızda gösterin.
3. **Buton aksiyonlarını yönetin**: Kullanıcı etkileşimlerini uygulamanızın bu etkileşimlere verdiği yanıtlarla ilişkilendirin. Örneğin, kullanıcılar butonlara tıkladığında bağlantılar açın veya flow'u kapatın.

## Başlamadan önce \{#before-you-start\}

Başlamadan önce şu adımları tamamlayın:

1. Adapty Kontrol Paneli'nde [uygulamanızı Google Play'e bağlayın](initial-android).
2. Adapty'de [ürünlerinizi oluşturun](create-product).
3. [Bir flow oluşturun](paywall-builder-templates) ve [flow'a ürün ekleyin](paywall-product-block).
4. [Bir placement oluşturun ve flow'unuzu ekleyin](create-placement).
5. Uygulama kodunuza [Adapty SDK'yı yükleyip etkinleştirin](sdk-installation-android). Bu kılavuz Adapty Android SDK v4 API'larını kullanmaktadır.

:::tip
Bu adımları tamamlamanın en hızlı yolu [hızlı başlangıç kılavuzunu](quickstart) takip etmek ya da [Developer CLI](developer-cli-quickstart) kullanarak flow'lar ve placement'lar oluşturmaktır.
:::

## 1. Flow'u alın \{#get-the-flow\}

Flow'larınız, kontrol panelinde yapılandırılmış placement'larla ilişkilendirilmiştir. Placement'lar, farklı kitleler için farklı flow'lar çalıştırmanıza veya [A/B testleri](ab-tests) yapmanıza olanak tanır.

Adapty Flow & Paywall Builder'da oluşturulan bir flow'u almak için şunları yapmanız gerekir:

1. `getFlow` metodunu kullanarak [placement](placements) ID'sine göre `flow` nesnesini alın ve bir görünüm yapılandırmasına sahip olup olmadığını kontrol edin.

2. `getFlowConfiguration` metodunu kullanarak görünüm yapılandırmasını alın. Görünüm yapılandırması, flow'u görüntülemek için gereken UI öğelerini ve stil bilgilerini içerir.

:::important
Görünüm yapılandırmasını almak için [flow'u yayınlayın](builder-save-publish). Yayınlanmamış düzenlemeleri olan bir flow **Dirty** statüsüne sahip olur ve placement'ı son yayınlanan versiyonu sunmaya devam eder.
:::

<Tabs groupId="current-os" queryString>

<TabItem value="kotlin" label="Kotlin" default>

```kotlin showLineNumbers
Adapty.getFlow("YOUR_PLACEMENT_ID") { result ->
    if (result is AdaptyResult.Success) {
        val flow = result.value
        
        if (!flow.hasViewConfiguration) {
            return@getFlow
        }
        
        AdaptyUI.getFlowConfiguration(flow) { configResult ->
            if (configResult is AdaptyResult.Success) {
                val flowConfiguration = configResult.value
            }
        }
    }
}
```
</TabItem>
<TabItem value="java" label="Java" default>

```java showLineNumbers

Adapty.getFlow("YOUR_PLACEMENT_ID", result -> {
    if (result instanceof AdaptyResult.Success) {
        AdaptyFlow flow = ((AdaptyResult.Success<AdaptyFlow>) result).getValue();
        
        if (!flow.hasViewConfiguration()) {
            return;
        }
        
        AdaptyUI.getFlowConfiguration(flow, configResult -> {
            if (configResult instanceof AdaptyResult.Success) {
                AdaptyUI.FlowConfiguration flowConfiguration =
                    ((AdaptyResult.Success<AdaptyUI.FlowConfiguration>) configResult).getValue();
                // use loaded configuration
            }
        });
    }
});
```
</TabItem>

</Tabs>

## 2. Flow'u göster \{#display-the-flow\}

Artık flow yapılandırmanız hazır olduğunda, flow'unuzu görüntülemek için birkaç satır kod eklemeniz yeterlidir.

Görsel flow'u cihaz ekranında gösterebilmek için önce yapılandırmanız gerekir. Bunun için `AdaptyUI.getFlowView()` metodunu çağırın ya da doğrudan `AdaptyFlowView` oluşturun:

<Tabs groupId="current-os" queryString>
  <TabItem value="kotlin" label="Kotlin (seçenek 1)" default>

```kotlin showLineNumbers
   val flowView = AdaptyUI.getFlowView(
       activity,
       flowConfiguration,
       null, // products = null means auto-fetch
       eventListener,
   )
```
</TabItem>
<TabItem value="kotlin2" label="Kotlin (option 2)" default>

```kotlin showLineNumbers
   val flowView =
        AdaptyFlowView(activity) // or retrieve it from xml
   ...
   with(flowView) {
       showFlow(
           flowConfiguration,
           null, // products = null means auto-fetch
		   eventListener,
       )
   }
```

</TabItem>
<TabItem value="java" label="Java (option 1)" default>

```java showLineNumbers
AdaptyFlowView flowView = AdaptyUI.getFlowView(
        activity,
        flowConfiguration,
        null, // products = null means auto-fetch
        eventListener
);
```
</TabItem>
<TabItem value="java2" label="Java (seçenek 2)" default>

```java showLineNumbers
AdaptyFlowView flowView =
  new AdaptyFlowView(activity); //add to the view hierarchy if needed, or you receive it from xml
...
flowView.showFlow(flowConfiguration, products, eventListener);
```

</TabItem>
<TabItem value="XML" label="XML" default>

```xml showLineNumbers
<com.adapty.ui.AdaptyFlowView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" />
```
</TabItem>
</Tabs>

View başarıyla oluşturulduktan sonra, view hiyerarşisine ekleyebilir ve cihaz ekranında görüntüleyebilirsiniz.

:::tip
Bir flow'un nasıl görüntüleneceğine ilişkin daha fazla ayrıntı için [kılavuzumuza](android-present-paywalls) bakın.
:::

## 3. Buton eylemlerini yönetme \{#handle-button-actions\}

Kullanıcılar flow içindeki butonlara tıkladığında, Android SDK satın alma, geri yükleme, flow'u kapatma ve bağlantı açma işlemlerini otomatik olarak gerçekleştirir.

Ancak bazı butonların özel veya önceden tanımlanmış ID'leri vardır ve bu eylemler kodunuzda ele alınmalıdır. Ya da varsayılan davranışlarını geçersiz kılmak isteyebilirsiniz.

Örneğin, aşağıda kapat butonunun varsayılan davranışı gösterilmektedir. Bunu koda eklemeniz gerekmez, ancak gerekirse nasıl yapıldığını buradan görebilirsiniz.

:::tip
Buton [action'ları](android-handle-paywall-actions) ve [event'leri](android-handling-events) nasıl yöneteceğinizi anlatan kılavuzlarımızı okuyun.
:::

<Tabs groupId="current-os" queryString>

<TabItem value="kotlin" label="Kotlin" default>

```kotlin showLineNumbers title="Kotlin"
override fun onActionPerformed(action: AdaptyUI.Action, context: Context) {
    when (action) {
        AdaptyUI.Action.Close -> (context as? Activity)?.onBackPressed() // default behavior
    }
}
```
</TabItem>
<TabItem value="java" label="Java" default>

```java showLineNumbers
@Override
public void onActionPerformed(@NonNull AdaptyUI.Action action, @NonNull Context context) {
    if (action instanceof AdaptyUI.Action.Close) {
        if (context instanceof Activity) {
            ((Activity) context).onBackPressed();
        }
    }
}
```
</TabItem>

</Tabs>

## Sonraki adımlar \{#next-steps\}

<SupportForum />

Paywall'ınız uygulamada görüntülenmeye hazır. Paywall üzerinden test satın alımı tamamlayabildiğinizden emin olmak için [Google Play Store'da satın alımlarınızı test edin](testing-on-android).

Şimdi, doğru kullanıcılara paywall gösterdiğinizden veya ücretli özelliklere erişim sağladığınızdan emin olmak için [kullanıcıların access level'ını kontrol etmeniz](android-check-subscription-status) gerekir.

## Tam örnek \{#full-example\}

Tüm bu adımları uygulamanızda nasıl bir araya getirebileceğiniz aşağıda gösterilmektedir.

<Tabs groupId="current-os" queryString>

<TabItem value="kotlin" label="Kotlin" default>

```kotlin showLineNumbers title="Kotlin"
class MainActivity : AppCompatActivity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)

        Adapty.getFlow("YOUR_PLACEMENT_ID") { flowResult ->
            if (flowResult is AdaptyResult.Success) {
                val flow = flowResult.value

                if (!flow.hasViewConfiguration) {
                    // Use custom logic
                    return@getFlow
                }

                AdaptyUI.getFlowConfiguration(flow) { configResult ->
                    if (configResult is AdaptyResult.Success) {
                        val flowConfiguration = configResult.value

                        val flowView = AdaptyUI.getFlowView(
                            this,
                            flowConfiguration,
                            null, // products = null means auto-fetch
                            object : AdaptyFlowDefaultEventListener() {
                                override fun onActionPerformed(action: AdaptyUI.Action, context: Context) {
                                    when (action) {
                                        is AdaptyUI.Action.Close -> {
                                            (context as? Activity)?.onBackPressed()
                                        }
                                    }
                                }
                            }
                        )

                        setContentView(flowView)
                    }
                }
            }
        }
    }
}

```
</TabItem>
<TabItem value="java" label="Java" default>

```java showLineNumbers
public class MainActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);

        Adapty.getFlow("YOUR_PLACEMENT_ID", flowResult -> {
            if (flowResult instanceof AdaptyResult.Success) {
                AdaptyFlow flow = ((AdaptyResult.Success<AdaptyFlow>) flowResult).getValue();

                if (!flow.hasViewConfiguration()) {
                    // Use custom logic
                    return;
                }

                AdaptyUI.getFlowConfiguration(flow, configResult -> {
                    if (configResult instanceof AdaptyResult.Success) {
                        AdaptyUI.FlowConfiguration flowConfiguration =
                            ((AdaptyResult.Success<AdaptyUI.FlowConfiguration>) configResult).getValue();

                        AdaptyFlowView flowView = AdaptyUI.getFlowView(
                            this,
                            flowConfiguration,
                            null, // products = null means auto-fetch
                            new AdaptyFlowDefaultEventListener() {
                               @Override
                                    public void onActionPerformed(@NonNull AdaptyUI.Action action, @NonNull Context context) {
                                        if (action instanceof AdaptyUI.Action.Close) {
                                            if (context instanceof Activity) {
                                                ((Activity) context).onBackPressed();
                                            }
                                        }
                                    }
                            }
                        );

                        setContentView(flowView);
                    }
                });
            }
        });
    }
}

```
</TabItem>

</Tabs>
