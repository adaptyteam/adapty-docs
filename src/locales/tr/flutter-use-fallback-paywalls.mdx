---
title: "Flutter - Yedek paywall kullanımı"
description: "Kullanıcılar çevrimdışıyken veya Adapty sunucularına ulaşılamadığında bu durumları yönetin"
metadataTitle: "Flutter'da Yedek Paywall Kullanımı | Adapty Dokümanları"
---

import SampleApp from '@site/src/locales/tr/reusable/SampleApp.md'; 

import FallbackPaywallIntroduction from '@site/src/locales/tr/reusable/FallbackPaywallIntroduction.md';

:::warning
Yedek paywall'lar Flutter SDK v2.11 ve sonraki sürümlerde desteklenmektedir.
:::

<FallbackPaywallIntroduction />

## Yapılandırma \{#configuration\}

1. Yedek yapılandırma dosyalarını projenin kök dizinindeki `assets` klasörüne ekleyin.
2. Hedef paywall veya onboarding'i almadan **önce** `.setFallback` metodunu çağırın.

```javascript showLineNumbers title="javascript"

final assetId = Platform.isIOS ? 'assets/ios_fallback.json' : 'assets/android_fallback.json';

try {
  await Adapty.setFallback(assetId);
} on AdaptyError catch (adaptyError) {
  // handle the error
} catch (e) {
}
```

Parametreler:

| Parametre      | Açıklama                                                                                                                                                          |
| :------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **assetId**    | Yedek yapılandırma dosyasının yolu. |

<SampleApp />