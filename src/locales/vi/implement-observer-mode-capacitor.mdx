---
title: "Triển khai Observer mode trong Capacitor SDK"
description: "Triển khai observer mode trong Adapty để theo dõi các sự kiện gói đăng ký của người dùng trong Capacitor SDK."
metadataTitle: "Triển khai Observer Mode trong Capacitor SDK | Tài liệu Adapty"
---

Nếu bạn đã có hệ thống mua hàng riêng và chưa sẵn sàng chuyển hoàn toàn sang Adapty, bạn có thể tìm hiểu về [Observer mode](observer-vs-full-mode). Ở dạng cơ bản, Observer Mode cung cấp phân tích nâng cao và tích hợp liền mạch với các hệ thống attribution và phân tích.
Nếu điều này phù hợp với nhu cầu của bạn, bạn chỉ cần:
1. Bật tính năng này khi cấu hình Adapty SDK bằng cách đặt tham số `observerMode` thành `true`. Làm theo hướng dẫn cài đặt cho [Capacitor](sdk-installation-capacitor#activate-adapty-module-of-adapty-sdk).
2. [Báo cáo giao dịch](report-transactions-observer-mode-capacitor) từ cơ sở hạ tầng mua hàng hiện có của bạn lên Adapty.
:::tip
Trong SDK v4, bạn cũng có thể hiển thị các flow và paywall do Adapty render trong Observer mode: khi người dùng nhấn vào nút mua hàng hoặc khôi phục, SDK sẽ chuyển hành động đó sang code của bạn để bạn tự thực hiện việc mua hàng hoặc khôi phục. Xem [Hiển thị flow trong Observer mode](capacitor-present-flows-in-observer-mode).
:::
### Thiết lập chế độ Observer \{#observer-mode-setup\}

Bật chế độ Observer nếu bạn tự xử lý việc mua hàng và trạng thái gói đăng ký, đồng thời chỉ dùng Adapty để gửi các sự kiện gói đăng ký và analytics.

:::important
Khi chạy ở chế độ Observer, Adapty SDK sẽ không đóng bất kỳ giao dịch nào, vì vậy hãy đảm bảo bạn tự xử lý điều đó.
:::

```typescript showLineNumbers

try {
  await adapty.activate({
    apiKey: 'YOUR_PUBLIC_SDK_KEY',
    params: {
      observerMode: true // Enable observer mode
    }
  });
} catch (error) {
  console.error('Failed to activate Adapty:', error);
}
```
| Tham số | Mô tả |
| --- | --- |
| **observerMode** | Giá trị boolean kiểm soát [Observer mode](observer-vs-full-mode). Giá trị mặc định là `false`. |
## Sử dụng flow & paywall của Adapty trong Observer Mode \{#using-adapty-paywalls-in-observer-mode\}

Nếu bạn muốn sử dụng các tính năng flow, paywall và A/B test của Adapty, bạn hoàn toàn có thể — nhưng cần thêm một số cấu hình trong Observer mode. Dưới đây là những gì bạn cần thực hiện ngoài các bước đã nêu ở trên:

1. Hiển thị [paywall Remote Config](present-remote-config-paywalls-capacitor) như bình thường. Đối với flow và paywall được xây dựng trong Flow Builder, xem [Hiển thị flow trong Observer mode](capacitor-present-flows-in-observer-mode).
2. [Liên kết flow và paywall](report-transactions-observer-mode-capacitor) với các giao dịch mua hàng.
