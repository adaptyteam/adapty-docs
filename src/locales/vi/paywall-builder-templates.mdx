---
title: "Tạo một flow"
description: "Bắt đầu một flow mới từ template gallery tùy chỉnh hoặc một starter tối giản."
metadataTitle: "Tạo một flow | Flow & paywall | Tài liệu Adapty"
keywords: ['flow', 'tạo flow', 'template', 'flow builder', 'paywall builder']
---

Bạn có thể tạo một flow theo các cách sau:

- **[Từ mẫu có sẵn](#use-a-template)**: Bắt đầu với một flow hoàn chỉnh gồm nhiều màn hình và chỉnh sửa bất kỳ thành phần nào trong đó.
- **[Từ thiết kế Figma](#import-a-design-from-figma)**: Chọn các frame trong Figma, gửi lên Adapty, và chỉnh sửa kết quả dưới dạng flow.
- **[Với công cụ AI coding](flow-generator-skill)**: Tải skill flow-generator, mô tả flow bằng ngôn ngữ tự nhiên, và công cụ sẽ tạo flow thông qua Adapty CLI.
- **[Từ đầu](#start-from-scratch)**: Bắt đầu với một màn hình trắng duy nhất.
- **[Từ paywall cũ](#migrate-a-paywall-from-the-legacy-paywall-builder)**: Tái tạo lại paywall từ Paywall Builder thành một flow, bao gồm layout, văn bản, sản phẩm và các bản dịch.

<FlowVideoTutorials />

## Tạo flow \{#create-flow\}

<ZoomImage id="create-flow-menu.webp" width="300px" alt="Create flow menu with Start from scratch and Browse templates options" float="right" />

1. Mở trang [**Flows**](filter-flows).
2. Nhấn **Create flow**.
3. Chọn một tùy chọn:
   - **Browse templates** (mở thư viện Template)
   - **Start from scratch** (tạo flow trống)
4. Đổi tên flow trong trình chỉnh sửa. Nhấn vào tên flow ở thanh tiêu đề và nhập tên mới.

<ZoomImage id="rename-flow.webp" width="500px" alt="Rename the flow name in the top toolbar" />

:::warning
Adapty cho phép đặt tên flow trùng nhau. Hãy đổi tên cho mỗi flow mới, nếu không bạn sẽ tạo ra nhiều flow **Untitled** khó phân biệt.
:::

### Sử dụng template \{#use-a-template\}

<ZoomImage id="template-library.webp" width="700px" alt="Template library modal with template cards" />

Template library chứa nhiều template đóng vai trò là điểm khởi đầu cho flow của bạn. Mỗi template là một flow hoàn chỉnh với nhiều màn hình, các phần tử tương tác và điều hướng hoạt động sẵn. Bạn có thể chỉnh sửa bất kỳ phần tử nào để tùy chỉnh theo ý muốn.

Để áp dụng một template:

1. Trong Template library, duyệt qua các thẻ template. Mỗi thẻ hiển thị ảnh chụp màn hình xem trước từ một flow.
2. Nhấp **Use as template** trên thẻ bạn muốn chọn.

Template sẽ được tải trong builder. Từ đây, bạn có thể thay đổi bất kỳ phần tử, màn hình hoặc thuộc tính nào.

### Bắt đầu từ đầu \{#start-from-scratch\}

<ZoomImage id="empty-screen.webp" width="300px" alt="Flow trống với một màn hình trắng duy nhất trong builder" float="right" />

Bắt đầu từ đầu tạo ra một flow với một màn hình trắng duy nhất. Thiết kế màn hình bằng các thành phần từ [thư viện thành phần](builder-elements).

## Nhập thiết kế từ Figma \{#import-a-design-from-figma\}

:::link
Bài viết chính: [Nhập thiết kế từ Figma](import-from-figma)
:::

Nếu bạn đã có mockup paywall trong Figma, bạn có thể gửi trực tiếp sang Adapty thay vì dựng lại màn hình từ đầu:

1. Cài đặt plugin Adapty cho Figma.
2. Chọn các frame và bật plugin.
3. Adapty nhận flow của bạn cùng với bố cục, văn bản và hình ảnh. Các thành phần tương tác cần được cấu hình thủ công.

## Migrate một paywall từ Paywall Builder cũ \{#migrate-a-paywall-from-the-legacy-paywall-builder\}

:::link
Bài viết chính: [Chuyển đổi một paywall thành flow](convert-paywall-to-flow)
:::

Thay vì tạo một flow từ template, bạn có thể chuyển đổi một paywall cũ mà bạn đã có sẵn.

Mở trang dashboard của paywall và nhấp vào **Move to new builder**. Adapty sẽ tạo lại paywall đó dưới dạng một draft flow mới — giữ nguyên bố cục, nội dung văn bản, sản phẩm và các bản dịch. Paywall cũ vẫn tiếp tục hoạt động để đảm bảo quá trình chuyển đổi diễn ra suôn sẻ.

<ZoomImage id="move-to-new-builder.webp" width="700px" alt="Move to new builder button in the paywall action row" />

## Thay đổi template \{#change-the-template\}

Bạn có thể đổi template ngay trong builder. Mở bảng Screens và nhấn nút **Templates** <Inline id="template-library.svg" alt="Templates" /> để mở lại thư viện Template, sau đó chọn template mới.

:::warning
Khi áp dụng template mới, bản nháp flow hiện tại của bạn sẽ bị thay thế. Adapty sẽ yêu cầu xác nhận — nhấn **Use template** để tiếp tục, hoặc **Cancel** để giữ lại bản nháp. Sau khi xác nhận, bản nháp cũ không thể khôi phục được. Flow đã publish vẫn hoạt động bình thường và không bị ảnh hưởng.
:::

## Phông chữ tùy chỉnh trong template \{#custom-fonts-in-templates\}

:::link
Bài viết chính: [Phông chữ tùy chỉnh trong Flow & Paywall Builder](using-custom-fonts-in-flow-builder)
:::

<ZoomImage id="custom-font-warning.webp" width="300px" alt="Custom font chip on a template card with the warning tooltip" float="right" />

Các template được đánh dấu với chip **Custom font** sử dụng phông chữ tùy chỉnh. Những phông chữ này không được đóng gói sẵn trong SDK. Hãy di chuột vào chip để xem template đó sử dụng những phông chữ nào.

Để hiển thị đúng kiểu chữ trên thiết bị, hãy thêm các file phông chữ vào app bundle của bạn. Các phiên bản app cũ chưa có phông chữ này sẽ tự động dùng phông chữ hệ thống thay thế.

Để thay đổi font mà không ảnh hưởng đến các phiên bản cũ, hãy [sao chép flow](copy-flows), thay đổi font trong bản sao, và giới hạn bản sao cho [các phiên bản ứng dụng có hỗ trợ font đó](segments).
