---
title: "Tối ưu hóa việc tải flow & paywall trong Flutter SDK"
description: "Tải Adapty flow và paywall một cách đáng tin cậy: thời điểm, bộ nhớ đệm và các mẫu dự phòng cho Flutter."
metadataTitle: "Tối ưu hóa việc tải Flow & Paywall | Flutter SDK | Tài liệu Adapty"
---

Một lần tải flow hoặc paywall đáng tin cậy trên Flutter cần đảm bảo ba điều: hiển thị nhanh, trả về biến thể đúng với đối tượng mục tiêu, và dự phòng hợp lý khi mạng chậm. Các quy tắc dưới đây bao gồm các mẫu về thời điểm, bộ nhớ đệm và dự phòng để đạt được điều đó.

:::tip
Các quy tắc này giả định rằng `Adapty().activate()` và `Adapty().identify()` đã hoàn tất. Xem [Thứ tự gọi trong Flutter SDK](flutter-sdk-call-order).
:::

## Quy tắc và lưu ý \{#rules-and-pitfalls\}

| Nên làm | Không nên làm | Lý do |
|---|---|---|
| Fetch placement mà bạn chuẩn bị hiển thị.                                                                                  | Prefetch tất cả placement đồng thời khi khởi động.        | Bulk prefetch chặn main thread và gây màn hình đen trong khi xử lý.                                  |
| Fetch `getFlow` sau khi attribution đã có cơ hội resolve — ví dụ, 1–2 giây sau `activate` hoặc sau khi `didUpdateProfileStream` kích hoạt. | Gọi `getFlow` trong `main()` trước `runApp`.         | Attribution chưa được xác định. Flow sẽ resolve theo đối tượng mặc định và âm thầm bỏ qua các phân khúc và cá nhân hóa ASA. |
| Đặt `loadTimeout` và cấu hình [paywall dự phòng](fallback-paywalls) cho mọi placement.                             | Chờ `getFlow` vô thời hạn.                     | Nếu không có timeout, người dùng kết nối kém sẽ thấy màn hình trắng cho đến khi mạng phản hồi — hoặc tắt ứng dụng.     |

Xem [Tải paywall và sản phẩm](fetch-paywalls-and-products-flutter) để tham khảo thông số `fetchPolicy` và `loadTimeout`, và [Placements](placements) để chọn placement phù hợp.

## Tối ưu cho kết nối kém \{#tune-for-poor-connectivity\}

Đối với các thị trường có kết nối kém liên tục (khu vực nông thôn, phương tiện giao thông, vùng bị ảnh hưởng bởi định tuyến mạng):

- Đặt `fetchPolicy: AdaptyFlowFetchPolicy.returnCacheDataElseLoad` cho mọi lần fetch ngoại trừ lần đầu tiên.
- Cấu hình [paywall dự phòng](fallback-paywalls) cho mọi placement trên Adapty Dashboard.
- Đặt `loadTimeout` từ 3–5 giây và chấp nhận dùng fallback khi timeout xảy ra.
- Đừng chặn việc hiển thị flow vì `getProfile()`. Gọi `getFlow` độc lập để một profile tải chậm không làm tắc nghẽn giao diện.
