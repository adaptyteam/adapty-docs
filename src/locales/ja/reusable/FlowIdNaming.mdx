---
no_index: true
---

import Callout from '../../../components/Callout.astro';

<Callout type="note">
デフォルトでは、これらのIDは自動生成されます。そのため、アプリが受け取るのは `welcome` ではなく `scr_oAPBHPa7` のような値になります。アナリティクスでIDを読み取る必要がある場合は、独自のIDを設定してください。フロービルダーで入力欄や選択可能なグループに名前を付け、flow-generatorスキルでスクリーンの名前を変更します。詳しくは[アナリティクスが認識するIDに名前を付ける](flow-analytics-ids)を参照してください。
</Callout>
