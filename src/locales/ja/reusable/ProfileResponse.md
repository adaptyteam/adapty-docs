<p> </p>

リクエストは成功しました。レスポンスボディには `data` フィールドが含まれており、ユーザーのプロファイルと関連情報がカプセル化されています。

| パラメータ | 型     | Null許容           | 説明                                                         |
| ---------- | ------ | ------------------ | ------------------------------------------------------------ |
| data       | Object | :heavy_minus_sign: | ユーザーの詳細とメタデータを含む [Profile](server-side-api-objects#profile) オブジェクトが格納されます。 |

### data オブジェクトの構造 \{#data-object-structure\}

`data` フィールドはユーザープロファイルのメインコンテナです。以下のフィールドが含まれます：