---
title: "Flutter - フォールバックペイウォールを使用する"
description: "ユーザーがオフラインのときや Adapty サーバーが利用できないときの対処方法"
metadataTitle: "Flutter でフォールバックペイウォールを使用する | Adapty ドキュメント"
---

import SampleApp from '@site/src/locales/ja/reusable/SampleApp.md'; 

import FallbackPaywallIntroduction from '@site/src/locales/ja/reusable/FallbackPaywallIntroduction.md';

:::warning
フォールバックペイウォールは Flutter SDK v2.11 以降でサポートされています。
:::

<FallbackPaywallIntroduction />

## 設定 \{#configuration\}

1. フォールバック設定ファイルをプロジェクトルートの `assets` ディレクトリに追加します。
2. 対象のペイウォールまたはオンボーディングを取得する**前に** `.setFallback` メソッドを呼び出します。

```dart showLineNumbers title="Flutter"

final assetId = Platform.isIOS ? 'assets/ios_fallback.json' : 'assets/android_fallback.json';

try {
  await Adapty().setFallback(assetId);
} on AdaptyError catch (adaptyError) {
  // handle the error
} catch (e) {
}
```

パラメーター：
| パラメーター | 説明 |
| :------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **assetId**    | フォールバック設定ファイルへのパス。 |

<SampleApp />