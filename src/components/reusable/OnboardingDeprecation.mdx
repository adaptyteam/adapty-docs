---
no_index: true
---
import Callout from '../Callout.astro';

<Callout type="warning">
**Onboardings are deprecated starting with Adapty SDK v4.** Build [flows](adapty-flow-builder) instead: unlike onboardings, which run inside a WebView, flows render natively on the device — smoother animations, a consistent native look and feel, faster load times, and no WebView runtime dependency.
</Callout>
