---
no_index: true
---

import Callout from '../Callout.astro';

<Callout type="note">
By default these IDs are generated, so your app receives `scr_oAPBHPa7` rather than `welcome`. If your analytics depends on reading them, set your own: name inputs and selectable groups in the Flow Builder, and rename screens with the flow-generator skill. See [Name the IDs your analytics sees](flow-analytics-ids).
</Callout>
