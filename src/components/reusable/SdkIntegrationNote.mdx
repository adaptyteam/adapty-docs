---
no_index: true
---
import Callout from '../Callout.astro';

<Callout type="note">
If you use a third-party user ID as your Customer User ID, don't pass it during `activate()` — the third-party SDK may not have generated it yet. Instead, call `activate()` without a CUID first, then `setIntegrationIdentifier()`, then `identify()` with the CUID.
</Callout>
